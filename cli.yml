dependencies:
  typescript:
    - "npm i --save-dev @types/node typescript ts-node"
    - "npm i --save-dev prettier tslint tslint-config-prettier tslint-plugin-prettier"
  routing-controllers:
    - "npm i --save routing-controllers typedi reflect-metadata koa koa-router koa-bodyparser koa-multer class-validator"
    - "npm i --save-dev @types/koa @types/koa-router @types/koa-bodyparser"
  test:
    - "npm i --save-dev chai mocha sinon supertest @types/chai @types/mocha @types/sinon @types/supertest"
  request:
    - "npm i --save-dev @types/request @types/request-promise-native"
    - "npm i --save request request-promise-native"
  winston:
    - "npm i --save app-root-path winston"
    - "npm i --save-dev @types/app-root-path "

