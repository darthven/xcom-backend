export interface Credentials {
    /**
     * Логин магазина, полученный при подключении
     */
    userName: string

    /**
     * Пароль магазина, полученный при подключении
     */
    password: string
}
